# Agentic Doc RAG System - example environment file

# Choose which LLM provider to use: 'gemini' or 'openai'
LLM_PROVIDER=gemini

# Gemini API key (required if LLM_PROVIDER=gemini)
GOOGLE_API_KEY=your_gemini_key_here

# OpenAI API key (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_key_here

# Optional bearer token for securing the /hackrx/run endpoint
HACKATHON_BEARER_TOKEN=changeme
